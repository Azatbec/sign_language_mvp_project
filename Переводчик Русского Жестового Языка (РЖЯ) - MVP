Подробная инструкция по управлению двумя приложениями: Переводчик РЖЯ (MVP)
Здравствуйте! На основе предоставленного кода, я подготовил подробную инструкцию по управлению вашими двумя приложениями. Эти приложения являются частью MVP-системы для распознавания и перевода русского жестового языка (РЖЯ), ориентированной на дактильную азбуку (жесты для букв русского алфавита).
Приложения:

Основной режим (Переводчик): Распознавание жестов с камеры в реальном времени и преобразование в текст. Также поддерживает обратный режим (показ жестов по тексту).
Тестовый/обучающий режим: Ввод текста и показ соответствующих изображений жестов для обучения или тестирования.

Инструкция охватывает запуск, интерфейс, управление клавишами и советы. Предполагается, что у вас установлены все зависимости (Python, OpenCV, PyTorch и т.д.), модель (best_model_fixed.pth) и изображения жестов в папке alfavit_parts/. Если что-то не работает, проверьте консоль на ошибки.
1. Общие требования и запуск
Требования

Python: 3.8+ с библиотеками: opencv-python, torch, numpy, mediapipe, pillow, tkinter, pyperclip.
Аппаратное: Веб-камера для основного режима; GPU рекомендуется для модели.
Файлы:
Модель: bukva/best_model_fixed.pth.
Изображения: alfavit_parts/{буква}.png (для 33 букв: Ё–Я).
Шрифт: arial.ttf (для кириллицы; если нет, используется системный).


Запуск через GUI
Основной файл: new_mvp_model.py (он запускает GUI с выбором режимов).

Команда: python new_mvp_model.py (или запустите .exe, если собрано PyInstaller).
Окно GUI:
Заголовок: "Переводчик РЖЯ".
Вкладки:
🖐 Переводчик: Кнопка "🚀 Запустить переводчик" — запускает основной режим (mvp_model.py).
🧪 Тестирование: Кнопка "🎯 Запустить тестовый режим" — запускает тестовый режим (test_mode2.py).

Футер: "© Проект РЖЯ • MVP 2026" и кнопка "✖ Выход".

Закрытие: Кнопка "✖ Выход" или крестик окна.

Прямой запуск скриптов (без GUI)

Основной режим: python mvp_model.py.
Тестовый режим: python test_mode2.py.

Примечание: При запуске через PyInstaller (.exe) ресурсы упакованы, так что пути к файлам обрабатываются автоматически.
2. Основной режим (Переводчик: Жесты → Текст и Текст → Жесты)
Это приложение использует камеру для распознавания жестов одной руки (MediaPipe для трекинга, PyTorch-модель для классификации). Поддерживает 33 буквы (Ё–Я). Окно: "Дактиль РЖЯ - Реальное время" (размер 1280x720).
Интерфейс

Верхняя часть:
Режим (зеленый/желтый/оранжевый текст: "Режим: Жесты → Текст (1)", etc.).
"Распознано:" + текущая буква (зеленая, если распознано; серый текст "Покажите жест", если нет).

Средняя часть: "Текст:" + накопленный текст (с переносами строк; черный шрифт, если текст есть; серый плейсхолдер "(начните дактилировать)", если пусто).
Нижняя часть: Статус "Задержка: X кадров | 1/2/3 - режимы | + / - задержка" (желтый текст).
Правая сторона (в режиме чтения): Показ изображения жеста + прогресс-бар.
Камера: Видео с камеры + наложенные landmarks (точки на руке) для трекинга.

Режимы работы

Режим ввода (Жесты → Текст, по умолчанию, зеленый):
Показывайте жесты перед камерой (одна рука, хорошее освещение, расстояние 50–100 см).
Модель анализирует последовательность кадров (SEQ_LEN=32), нормализует и предсказывает букву.
Буква добавляется в текст после стабильности (CONFIRM_FRAMES кадров, по умолчанию 15).
Если рука не обнаружена, буфер очищается.

Режим чтения (Текст → Жесты, желтый):
Активируется, если текст не пуст.
Фиксирует фон, показывает изображения жестов по буквам текста (с задержкой READ_DELAY=45 кадров ~1.5 сек).
Прогресс-бар внизу + текст "Жест: {буква}" + счетчик "{текущая}/{всего}".
После показа всех букв возвращается в режим ввода.

Режим паузы (оранжевый): Ничего не происходит (камера активна, но обработка приостановлена).

Управление клавишами

Режимы:
1: Включить режим ввода.
2: Включить режим чтения (только если текст есть).
3: Включить паузу.

Редактирование текста (в режиме ввода):
Backspace (или код 8): Удалить последнюю букву.
Пробел (код 32): Добавить пробел.
Enter (код 13): Добавить новую строку.
C (верхний/нижний регистр): Очистить весь текст.

Настройки:
+ или =: Увеличить задержку подтверждения (макс. 40 кадров).
-: Уменьшить задержку (мин. 5 кадров). Полезно для калибровки скорости распознавания.

Выход: Esc (код 27).

Советы

Держите руку неподвижно для стабильного распознавания.
Если модель ошибается, проверьте освещение или увеличьте задержку.
Текст оборачивается автоматически (максимальная ширина — ширина окна минус отступы).
Ошибки: Если нет камеры — приложение не запустится. Если модель не загружена — ошибка в консоли.

3. Тестовый/обучающий режим (Показ жестов по тексту)
Это приложение для ввода текста и показа изображений жестов. Полезно для обучения дактильной азбуке или тестирования. Окно: "Дактильная азбука РЖЯ" (размер 1280x720).
Интерфейс

Левая панель: "Введённый текст" + отображение текста (с переносами, прокруткой; серый плейсхолдер, если пусто).
Правая панель: "Жест РЖЯ" + изображение жеста + номер "{текущий}/{всего}" (фиолетовый текст).
Нижняя панель: Подсказки по клавишам + кнопки (виртуальные: синяя "1. Ввести текст", зеленая "2. Редактировать", красная "0. Очистить", оранжевая "ESC. Выход").
Если текста нет: Правая панель показывает "Нет букв РЖЯ".

Функции

Ввод/редактирование текста: Открывает окно Tkinter с полем для текста (скроллинг, шрифт Arial 18).
Показ жестов: Только для поддерживаемых букв (игнорирует другие символы, регистр не важен).
Прокрутка текста: Если текст длинный, используйте клавиши для сдвига.
Буфер обмена: Вставка из клипборда.

Управление клавишами

Текст:
1 или 2: Открыть редактор (ввод/редактирование; сохраните Ctrl+S или Enter).
0: Очистить текст.
V (или рус. М): Вставить текст из буфера обмена.

Навигация по жестам:
4 или ←: Предыдущий жест.
6 или →: Следующий жест.

Прокрутка текста:
8: Вверх (на одну строку).
5: Вниз (на одну строку).

Выход: Esc или 3.

Советы

В редакторе: Поддерживает многострочный текст, но жесты показываются только для букв А–Я, Ё.
Если жест не загружен: Не отображается (проверьте папку alfavit_parts/).
Прокрутка нужна для длинных текстов (макс. видимых строк ~10–12).
Ошибки: Если изображения не найдены — консоль покажет предупреждения.

4. Общие рекомендации и отладка

Производительность: На CPU может быть задержка; используйте CUDA (если доступно).
Ошибки:
"No such file": Проверьте пути к модели/изображениям.
"No camera": Подключите веб-камеру.
"Module not found": Установите зависимости.

Расширение: Добавьте поддержку слов (обновите модель) или TTS для озвучивания.
Безопасность: Приложения не используют интернет; все локально.
